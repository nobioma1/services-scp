import { Flex } from '@chakra-ui/react';

const Header = () => {
  return (
    <Flex
      width="100%"
      height="60px"
      alignItems="center"
      justifyContent="center"
      borderBottomWidth={1}
    >
      Header
    </Flex>
  );
};

export default Header;
